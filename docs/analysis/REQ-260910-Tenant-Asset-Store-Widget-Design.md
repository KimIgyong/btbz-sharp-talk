# REQ-260910 — 테넌트별 저장 공간 · 위젯 디자인 파일 · 개별 커스텀(폰트·아이콘·크기)

- 요청일: 2026-09-10 · 원문: "테넌트별로 폴더 생성(별도 보관할 설정 저장 공간) / 테넌트별 프론트엔드 채팅위젯 디자인
  파일 별도 생성·저장 / 기본 위젯 테마 설정 외에 테넌트 고유 디자인 컨셉·아이콘·화면 사이즈·폰트 등 개별 커스텀과
  관련 파일 저장·관리 기능. 사이드임팩트를 고려해 정리하고 최적안 제시"
- 산출: 이 REQ(현황·해석·갭·제약) + `PLN-260910-Tenant-Asset-Store-Widget-Design.md`(대안 비교·최적안·단계·와이어프레임)
- 관련: PLN-260818-Widget-Theme-And-Tab-Guide(브랜드색 램프) · PLN-260819-Widget-Theme-Extension(로고·런처) ·
  PLN-260819-Embed-SDK-Foundations(오리진 게이트) · PLN-260814(첨부 볼륨) · PLN-260817-Widget-Figma-Design(디자인 토큰)

## 1. AS-IS

### 1.1 테넌트별 파일 저장 — "폴더"는 있으나 규약이 없다
단일 볼륨 `UPLOAD_DIR`(스테이징 `/data/uploads`, 볼륨 `staging_ivy_uploads_staging`) 아래에 기능별로 제각각의 경로:

| 영역 | 경로 규칙 | 등록부(DB) | 서빙 |
|---|---|---|---|
| 채팅 첨부 | `{tenantId}/{YYYY-MM}/{uuid}.{ext}` (+`_t.webp`) | `message_attachments` | 서명 URL 15분/외부 7일 |
| 위젯 로고 | `{tenantId}/branding/{id}.{ext}` | `tenants.widget_theme.logo` (JSON) | 공개 `GET /tenants/widget-branding/logo?v=`, `v`=id 일치 시 immutable |
| AI 인제스트 원본 | `kb-ingest/{tenantId}/…` | `kb_ingest_*` | 내부 |
| 보드 첨부 | `board/{tenantId}/{uuid}{ext}` | `board_attachments` | 서명 URL |

- 테넌트 루트가 앞에 오는 경우(첨부·로고)와 기능이 앞에 오는 경우(kb-ingest·board)가 섞여 있어 **"테넌트 폴더 하나"로
  백업·용량 산정·이관·삭제(DSAR/테넌트 해지)를 할 수 없다.**
- API 이미지는 배포마다 재빌드 — 볼륨 밖의 파일은 다음 배포에 사라진다(compose 주석). **production compose(`docker/production/docker-compose.production.yml`)에는 API 컨테이너의 uploads 볼륨이 없다**(§4 SI-1) — 그대로 배포하면 로고·첨부가 재배포마다 사라진다.
- 파일 등록부가 기능별 테이블/JSON에 분산 — 테넌트 단위 용량·목록·정리 API 없음.

### 1.2 위젯 디자인 커스터마이즈 — 현재 가능한 것
`tenants.widget_theme`(JSON) → `normalizeWidgetTheme` → `session/ensure` 응답 → 위젯이 `:root` CSS 변수로 적용 +
shop별 localStorage 캐시(첫 페인트) → 로더 `embed.js`가 iframe 프레임 크기·위치 관리.

| 항목 | 현재 | 저장 |
|---|---|---|
| 브랜드색 | 1색 → 9단 램프 + 전경색(대비 4.5:1) | `brand` |
| 헤더 | white / brand | `headerStyle` |
| 로고 | png/jpg/webp ≤1MB, 1000×400, sharp 재인코딩, **svg 금지** | `logo{id,ext,mime,w,h}` + 파일 |
| 런처 | 위치 left/right · 크기 sm/md/lg(48/56/64) · 아이콘 chat/question/headset/logo | `launcher` |
| 문구 | 인사말·표시명 등 6언어 | `widget_copy` |
| 탭 | 노출 탭·위치 | `widget_tabs` |

### 1.3 요청한 항목의 현 상태
| 요청 | 현재 | 비고 |
|---|---|---|
| 폰트 | **고정** — `Pretendard`(jsDelivr CDN) + 시스템 폴백, `index.html`·Tailwind 하드코딩 | 테넌트별 폰트·크기 불가 |
| 아이콘 | 런처 4종 enum, 위젯 내부는 lucide 고정 | 테넌트 아이콘 파일 업로드 불가 |
| 화면 사이즈 | 로더 상수 `OPEN 444×680`, 패널 404px, 런처 3단 | 테넌트별 폭·높이 불가 |
| 디자인 컨셉 문서/시안 | 저장처 없음 | Figma 산출물은 `docs/`(개발자용)에만 |
| 설정 백업/이관 | 없음 — JSON 컬럼 5종은 DB에만 | 테넌트 단위 내보내기/가져오기 불가 |
| 커스텀 CSS/JS | 없음(의도) | 로고 svg 금지와 같은 이유 — iframe이 API와 동일 오리진 |

## 2. 요구 해석 (TO-BE)

| # | 요구 | 해석 |
|---|---|---|
| R1 | 테넌트별 폴더 | 볼륨 안 **테넌트 루트 규약** `tenants/{tenantId}/{area}/…` + 파일 **등록부 테이블**(`tenant_assets`)로 목록·용량·삭제·이관을 테넌트 단위로 가능하게. 기존 경로는 그대로 두고(이동 없음) 신규 영역만 규약을 따른다 |
| R2 | 위젯 디자인 파일 별도 저장 | `area=design`: 폰트(woff2)·아이콘(png/webp)·이미지·디자인 시안/가이드(pdf/png, 참고용). 콘솔 설정 > 위젯 "디자인 파일" 관리(업로드·목록·미리보기·삭제·다운로드) |
| R3 | 개별 커스텀(폰트·아이콘·크기) | `widget_theme`를 **구조화 토큰**으로 확장: 폰트(프리셋 또는 업로드 자산)·기본 글자 크기·모서리·패널 폭/높이(범위 제한)·런처/탭 아이콘(자산 참조). 위젯은 CSS 변수로, 로더는 프레임 크기로 소비. **자유 CSS/JS는 채택하지 않음**(§4 SI-5) |
| R4 | "별도 보관해야 할 설정" | `area=settings`: 테넌트 설정 스냅샷(위젯 테마·문구·탭·알림 채널·임베드 오리진·지식 옵션 + 자산 목록)을 JSON으로 **내보내기/가져오기** — 이관·복구·감사 목적 |

## 3. 갭 분석

| 갭 | AS-IS | TO-BE | 성격 |
|---|---|---|---|
| G1 저장 규약·등록부 | 기능별 경로, 등록부 분산 | `tenants/{id}/{area}` + `tenant_assets` | 스키마 1테이블 + 저장 유틸 |
| G2 자산 API | 로고 전용 업로드만 | 범용 업로드/목록/삭제/서빙(공개 immutable·비공개 서명) + 종류별 검증(매직바이트·크기·픽셀) + 테넌트 용량 상한 | API |
| G3 디자인 토큰 | 색·헤더·로고·런처 | + 폰트·글자 크기·모서리·패널 크기·아이콘 | types 정규화 + 위젯 CSS 변수 + 로더 프레임 |
| G4 콘솔 | 위젯 테마 카드 | + 디자인 파일 카드 + 디자인 프로필 항목 + 미리보기 확장 | web |
| G5 설정 스냅샷 | 없음 | export/import + 스냅샷 파일 보관 | API + web |
| G6 운영 | 볼륨·백업·용량 가시성 없음 | 테넌트별 용량 집계·어드민 표시(선택) | ops |

## 4. 사이드임팩트 (반드시 고려)

| # | 영역 | 영향 | 대응 |
|---|---|---|---|
| SI-1 | 볼륨/배포 | 파일은 볼륨에만 산다. **production compose에 API uploads 볼륨이 없음(실측)** — 이 요구와 무관하게 이미 로고·첨부 유실 결함 | P1 착수 전 `docker/production`에 `sharptalk_uploads_production:/data/uploads` + `UPLOAD_DIR` 추가(별건 FIX), `pre-deploy-check`에 볼륨 마운트 항목 |
| SI-2 | 기존 경로 | 로고·첨부·보드·인제스트 경로를 옮기면 서명 URL·`storage_path` 전부 깨짐 | **이동 금지**. 신규 영역만 새 규약. 등록부에 기존 로고를 "가상 등록"(경로 참조)해 테넌트 용량 합산에만 포함 |
| SI-3 | 위젯 캐시·버전 스큐 | localStorage 테마 캐시(shop별)·로더 런처 캐시에 새 필드 추가 시 구/신 위젯 혼재 | 정규화가 미지 필드 무시(현행) + 새 필드는 optional·기본값 → 구 위젯은 무시, 신 위젯은 구 캐시 허용. `embed.js`는 no-cache라 로더 스큐 없음 |
| SI-4 | 로더 프레임 | 패널 폭/높이를 테넌트별로 바꾸면 로더 `OPEN` 상수와 위젯 패널 CSS **양쪽 합의** 필요(PLN-260819 W4 함정 재발 방지) | `ivy:launcher` 보고 메시지에 `frame{w,h}` 동봉 → 로더가 캐시·적용. 범위 제한(폭 360~480, 높이 560~760, `min(…,100vw/vh)` 유지). 앱모드(WebView)는 프레임을 네이티브가 잡으므로 무시 |
| SI-5 | 보안 | 커스텀 CSS/JS·SVG는 iframe이 API 오리진과 같아 세션 토큰·API 접근 벡터. 폰트 파일도 실행 벡터는 아니나 CORS 필요 | **자유 CSS/JS 비채택**, svg 금지 유지, 폰트는 woff2/ttf만(매직바이트)·CORS 헤더는 자산 라우트에만, 이미지는 sharp 재인코딩 |
| SI-6 | 성능/첫 페인트 | 커스텀 폰트는 다운로드 전 폴백 폰트로 렌더(FOUT) | `font-display: swap` + 프리셋 우선 권장, 폰트 파일 ≤2MB, 테마 캐시로 첫 페인트 유지 |
| SI-7 | 접근성 | 글자 크기·대비는 램프 계산이 이미 보장. 폰트 변경은 대비 무관 | 기본 글자 크기 13~16px 범위 제한 |
| SI-8 | 프라이버시/보존 | 디자인 자산·설정 스냅샷은 PII 아님. 설정 스냅샷에 자격증명은 **포함 금지**(연동 토큰·임베드 시크릿) | export 화이트리스트 필드만. 보존 축(대화 로그 400일)과 분리, 테넌트 해지 시 폴더 단위 삭제 가능해짐(장점) |
| SI-9 | 권한·감사 | 위젯 테마와 같은 master/director. 업로드·삭제·스냅샷 복원은 감사 | `AuditService.write` 필수 |
| SI-10 | 모바일 SDK·PWA | 위젯 iframe 내부 토큰은 그대로 적용. 프레임 크기는 앱모드 무시 | `?mode=app`에서 `frame` 무시 명시 |
| SI-11 | 매뉴얼·i18n | 설정 > 위젯 절 갱신, 6언어 키 | RPT에 재캡처 목록 |
| SI-12 | 용량·비용 | 무제한 업로드는 볼륨 고갈 | 테넌트별 상한(예: design 50MB·settings 20MB), 어드민 집계 |

## 5. 제약·결정 근거
- 위젯은 **단일 빌드**를 모든 테넌트가 공유한다(iframe SPA). 테넌트별 "파일로 디자인을 교체"하는 모델(테마 번들·CSS)은
  빌드 모델·보안 경계와 충돌 → 커스텀은 **데이터(토큰+자산 참조)**로, 파일은 **자산(폰트·아이콘·이미지·시안)**으로.
- 자산 URL은 **버전이 URL에 있을 때만 immutable**(로고 규칙 승계).
- 아메바 철학(적정기술): 등록부 1테이블·라우트 1세트로 로고를 포함한 모든 파일 종류를 다루고, 로고 서비스는 후속에 등록부로
  수렴(이번엔 무이동).

## 6. 사용자 플로우(TO-BE 요약)
```
설정 > 위젯
  [디자인 파일] 업로드(폰트/아이콘/이미지/시안) → 목록·용량 → 삭제
  [위젯 테마] 색·헤더·로고·런처(기존) + 폰트(프리셋|업로드 폰트)·글자 크기·모서리·패널 크기·런처/탭 아이콘(업로드 아이콘)
      → 미리보기 → 저장 → 위젯 다음 로드에 반영(테마 캐시)
설정 > 기타
  [설정 스냅샷] 내보내기(JSON 파일 저장·다운로드) / 가져오기(미리보기 → 적용) / 스냅샷 목록
```
